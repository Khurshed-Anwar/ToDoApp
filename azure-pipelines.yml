# ASP.NET Core (.NET Framework)
# Build and test ASP.NET Core projects targeting the full .NET Framework.
# Add steps that publish symbols, save build artifacts, and more:
# https://docs.microsoft.com/azure/devops/pipelines/languages/dotnet-core

trigger:
- main

pool:
  vmImage: 'windows-latest'

variables:
  solution: '**/*.slnx'
  buildPlatform: 'Any CPU'
  buildConfiguration: 'Release'

steps:
- task: NuGetToolInstaller@1

- task: NuGetCommand@2
  inputs:
    restoreSolution: '$(solution)'

- task: VSBuild@1
  inputs:
    solution: '$(solution)'
    msbuildArgs: '/p:DeployOnBuild=true /p:WebPublishMethod=Package /p:PackageAsSingleFile=true /p:SkipInvalidConfigurations=true /p:DesktopBuildPackageLocation="$(build.artifactStagingDirectory)\WebApp.zip" /p:DeployIisAppPath="Default Web Site"'
    platform: '$(buildPlatform)'
    configuration: '$(buildConfiguration)'

# Install Playwright Chromium browser
- task: PowerShell@2
  displayName: 'Install Playwright Browser'
  inputs:
    targetType: 'inline'
    script: |
      pwsh .\ToDoApp.Tests\bin\Release\net9.0\playwright.ps1 install chromium

- task: VSTest@2
  inputs:
    platform: '$(buildPlatform)'
    configuration: '$(buildConfiguration)'

# Copy Playwright test output into the artifact
- task: CopyFiles@2
  displayName: 'Copy Playwright Tests to Artifact'
  inputs:
    SourceFolder: '$(Build.SourcesDirectory)\ToDoApp.Tests\bin\Release\net9.0'
    Contents: '**'
    TargetFolder: '$(Build.ArtifactStagingDirectory)\PlaywrightTests'

- task: PublishBuildArtifacts@1
  inputs:
    PathtoPublish: '$(Build.ArtifactStagingDirectory)'
    ArtifactName: 'drop'
    publishLocation: 'Container'
